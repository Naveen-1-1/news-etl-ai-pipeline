# Core Dependencies
# Using latest Airflow version compatible with Python 3.13
apache-airflow>=3.1.0
pandas>=2.1.0
newsapi-python==0.2.7
python-dotenv==1.0.0

# Database - Using psycopg3 (modern, pure Python, no compilation needed)
psycopg[binary]>=3.1.0

# Additional utilities
requests>=2.31.0
sqlalchemy>=2.0.0  # ORM for database operations
